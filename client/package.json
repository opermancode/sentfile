{
  "name": "sentfile-client",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^6.5.0",       // latest stable
    "@testing-library/react": "^16.1.0",         // latest stable
    "@testing-library/user-event": "^14.6.1",    // latest stable
    "react": "^18.3.1",                          // latest stable
    "react-dom": "^18.3.1",                      // latest stable
    "react-scripts": "5.0.1",                    // still latest (CRA not updated)
    "web-vitals": "^4.2.0"                       // latest stable
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
